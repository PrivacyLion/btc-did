# secrets / runtime
keys/
var/
**/*.db
.venv/
__pycache__/
*.pyc
app/var/

# Client configuration with API keys (use clients.json.example as template)
clients.json

# Environment files with secrets
.env
.env.local
.env.production

# --- ANDROID / GRADLE ---
/.gradle/
/build/
/**/build/
.local.properties
local.properties
.cxx/
.cinterop/
captures/
.externalNativeBuild/
.idea/
/.kotlinc/
/.ksp/
*.iml

# NDK / JNI build outputs (keep jniLibs for pre-built .so files)
# /app/src/main/jniLibs/
/app/src/main/obj/

# Screenshots (don't commit to repo)
/Screenshot*.png

# macOS Finder metadata
**/.DS_Store
# OIDC signing keys (private - never commit)
keys/oidc_rs256.pem
